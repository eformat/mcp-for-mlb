apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mlb-agent-chainlit-data
  labels:
    app.kubernetes.io/name: mlb-agent
    app.kubernetes.io/component: chat-history
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
